**This Repo is used to Install the ECE agent on your nodes for Brownfeild Node Onboarding**

#prerequistes

1) all root Previlages should be provide to DAPO VM

2) node root details tobe provided and the root login to be enabled

3) have a Virtual Pyhton env with version 3.11 or greater and install paramiko labrary using below command
pip install paramiko

4) clone the repo and provide below inputs:
a. node details
b. DAPO VM details
c. Client secret
d. Client ID
e. TLS ID
